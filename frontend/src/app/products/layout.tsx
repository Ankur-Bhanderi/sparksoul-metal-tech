import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Premium Collections | SparkSoul Metal Tech Brass Bathroom Fittings",
  description: "Explore our signature collections of luxury basin mixers, showers, valves, and brass bathroom accessories.",
};

export default function ProductsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
